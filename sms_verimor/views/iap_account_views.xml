<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- IAP Account form – SMS Verimor credentials section -->
        <record id="view_iap_account_form_sms_verimor" model="ir.ui.view">
            <field name="name">iap.account.form.sms.verimor</field>
            <field name="model">iap.account</field>
            <field name="inherit_id" ref="iap.iap_account_form"/>
            <field name="arch" type="xml">
                <xpath expr="//sheet" position="inside">
                    <group string="SMS Verimor Credentials"
                           invisible="provider != 'sms_verimor'">
                        <field name="sms_username" placeholder="908501234567"/>
                        <field name="sms_password" password="True"/>
                        <field name="sms_source_addr" placeholder="FIRMAADI"/>
                        <div class="mt8">
                            <button name="action_check_sms_balance"
                                    string="Check SMS Balance"
                                    type="object"
                                    class="btn btn-secondary"
                                    invisible="provider != 'sms_verimor'"/>
                        </div>
                    </group>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
